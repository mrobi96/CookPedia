<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cookpedia">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".rating.ratingbar"></activity>
        <activity android:name=".menu.menuBook" />

        <activity
            android:name=".menu.DB_Parse"
            android:label="Bahan dan Cara"></activity>

        <activity android:name=".menu.DB_Parse_cemilan"/>
        <activity android:name=".menu.DB_Parse_makanan"/>

        <activity android:name=".menu.menuCemilan" />
        <activity android:name=".menu.menuMakanan" />
        <activity android:name=".menu.menuMinuman" />
        <activity android:name=".MenuAwal" />
        <activity android:name=".Splashbe.Splashbe">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AccountActivity.LoginActivity" />
        <activity android:name=".AccountActivity.MainActivity" />
        <activity android:name=".AccountActivity.SignupActivity" />
        <activity android:name=".AccountActivity.ResetPasswordActivity" />
    </application>

</manifest>